## REGULAR EXPRESSION ENGINE 

#### BUILD
Use make `make` utility to build and execute.

* To compile
    ```
    make NFA
    ```
* To execute
    ```
    make execute
    ```
* To clean all object files
    ```
    make clean
    ```
